/**
 * Default render for the @modal parallel slot: nothing. The slot is only filled
 * when the intercepting route (.)listas/[id] matches.
 */
export default function ModalDefault() {
  return null;
}
